# 项目协作约定

## 手动测试样本

- 供用户手动导入、验证功能的 JSON 样本统一直接放在仓库的 `json/` 文件夹。
- 使用能说明用途的文件名；不要将临时目录或随机子文件夹中的文件作为用户测试入口。
- 不覆盖用户已有样本；如文件名已存在，先确认内容是否相同，必要时使用新的描述性名称。
- 自动化测试内部如需临时文件隔离，可以使用临时目录，但交付给用户的样本必须放在 `json/`，回复中链接到该位置。
